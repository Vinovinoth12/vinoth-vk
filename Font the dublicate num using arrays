arr= [1,2,3,5,6,5,32];


print("Duplicate elements in given arrays:");

#searches for duplicate element

for i in range (0,len(arr)):
	for j in range(i+1,len(arr)):
		if(arr[i]==arr[j]):
			print(arr[j]);
